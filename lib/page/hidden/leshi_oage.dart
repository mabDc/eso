import 'package:flutter/material.dart';

class LeshiPage extends StatelessWidget {
  const LeshiPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("施工ing")),
      body: Container(
        child: Text("施工ing"),
      ),
    );
  }
}
